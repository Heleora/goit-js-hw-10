import './css/styles.css';

const DEBOUNCE_DELAY = 300;

// На практиці взагалі не використовуються запити типу GET https://bookstore.com/api/orders, лише через fetch()?
// if (!response.ok){} -> означає dbrjyfnb при будь-якій відповіді відмінній від 200? Навіть якщо будуть інші успішні відповіді 201, 202 тощо?

// https://restcountries.com/

